



export default function Page(){

    // server action to onboard the user
    // 200 || 201 dashboard/{id} ->200(login the user), 201->create new user
    // 404 ->some error have occured therefore redirect the user to sign-up route

    return (
        <div>
            Dashboard Page
        </div>
    )
}